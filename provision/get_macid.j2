{% for host in groups['masters'] -%}
   {{ hostvars[host].macid  }}
   {%- if not loop.last %},{% endif -%}
{%- endfor %}
,{% for host in groups['slaves'] -%}
   {{ hostvars[host].macid  }}
   {%- if not loop.last %},{% endif -%}
{%- endfor %}
