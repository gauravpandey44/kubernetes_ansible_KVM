{% for host in groups['masters'] -%}
   {{ hostvars[host].DISK  }}
   {%- if not loop.last %},{% endif -%}
{%- endfor %}
,{% for host in groups['slaves'] -%}
   {{ hostvars[host].DISK  }}
   {%- if not loop.last %},{% endif -%}
{%- endfor %}
