{% for host in groups['masters'] -%}
   {{ hostvars[host].CPU  }}
   {%- if not loop.last %},{% endif -%}
{%- endfor %}
,{% for host in groups['slaves'] -%}
   {{ hostvars[host].CPU  }}
   {%- if not loop.last %},{% endif -%}
{%- endfor %}
